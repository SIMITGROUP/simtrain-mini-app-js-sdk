// @generated by simtrain-sdk-generator. Do not edit.

export interface InvoiceDetailInput {
  /**
   * @maxLength 1000
   */
  description?: string;
  /**
   * Existing product id — the server builds the embedded snapshot. Charge items only (products whose categoryType is 'charge', e.g. registration or material fees). Class and recurring-fee products are invoiced through enrollment, which supplies the billing linkage this endpoint does not carry; sending one returns 400.
   * @maxLength 64
   */
  productId: string;
  /**
   * @minimum 1
   * @maximum 10000000
   * @example 4
   */
  quantity: number;
  /**
   * Stated explicitly by the caller (negotiated prices are real); never defaulted from the product.
   * @minimum 0
   * @maximum 10000000
   * @example 150
   */
  unitPrice: number;
}
