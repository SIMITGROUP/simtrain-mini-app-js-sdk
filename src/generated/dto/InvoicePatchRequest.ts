// @generated by simtrain-sdk-generator. Do not edit.

import type { InvoiceDetailInput } from "./InvoiceDetailInput";

export interface InvoicePatchRequest {
  /**
   * null clears the field.
   * @maxLength 1000
   */
  description?: string | null;
  /**
   * REPLACES the whole line list when present: send every line you want to keep. Absent leaves the existing lines untouched. Money fields are recomputed server-side.
   * @minItems 1
   * @maxItems 100
   */
  details?: InvoiceDetailInput[];
  /**
   * Not clearable.
   * @format date
   */
  invoiceDate?: string;
  /**
   * Existing student id; the server rebuilds the snapshot. Not clearable.
   * @maxLength 64
   */
  studentId?: string;
}
